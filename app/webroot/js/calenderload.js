// JavaScript Document
function setupCalendar() {
	Calendar.setup({
	  dateField      : 'date_from',
	  triggerElement : 'calendarButton_from'
	})
	Calendar.setup({
	  dateField      : 'date_to',
	  triggerElement : 'calendarButton_to'
	})
}